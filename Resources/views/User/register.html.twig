{% extends 'ANSUserBundle::layout.html.twig' %}

{% block title %}Регистрация на сайте{% endblock %}

{% block content %}

<h4>Регистрация на сайте</h4>

{% if message is defined %}

<p>Вы успешно зарегистрированы. По электронной почте вам отправлен пароль для входа на сайт</p>
<p><a href="{{ path('login') }}">Войти на сайт</a></p>

{% else %}

{{ form_start(form, { 'style': 'horizontal', 'col_size': 'sm', 'label_col': 4, 'widget_col': 8 }) }}
{{ form_errors(form, { 'global_errors': true }) }}

<div class="registration-email">{{ form_row(form.email) }}</div>
<div class="registration-name">{{ form_row(form.name) }}</div>
<div class="registration-website">{{ form_row(form.website) }}</div>
<div class="registration-submit">{{ form_row(form.register) }}</div>

{{ form_end(form) }}

{# Vs bots without JS #}
<script type="text/javascript">
    var markup = '<input type="checkbox" name="isjs" checked="checked" class="registration-accept" />';
    var div = document.getElementsByClassName('registration-website')[0];
    div.innerHTML += markup;
</script>

{% endif %}

{% endblock %}
